{
  "$schema": "../../node_modules/turbo/schema.json",
  "extends": ["//"],
  "tasks": {
    "turbo:build": {
      "cache": true,
      "dependsOn": ["^turbo:build"],
      "outputs": ["dist/**", "*.tsbuildinfo"],
    },
    "turbo:lint": {
      "cache": true,
      "dependsOn": ["^turbo:build"],
      "inputs": [
        "$TURBO_EXTENDS$",
        "$TURBO_ROOT$/.patricktree-stack/tooling/config-oxlint/**",
        "$TURBO_ROOT$/tooling/config-oxlint/**",
      ],
      "outputs": [],
    },
    "turbo:lint:fix": {
      "cache": true,
      "dependsOn": ["^turbo:build"],
      "inputs": [
        "$TURBO_EXTENDS$",
        "$TURBO_ROOT$/.patricktree-stack/tooling/config-oxlint/**",
        "$TURBO_ROOT$/tooling/config-oxlint/**",
      ],
      "outputs": ["src/**"],
    },
    "turbo:test": {
      "cache": true,
      "dependsOn": ["^turbo:build"],
      "outputs": [],
    },
  },
}
